# 🕵️‍♂️ نظام المفقودين الذكي (Smart Missing Persons System)

مرحباً بالفريق! هذا هو المستودع المركزي لمشروع تخرجنا. يحتوي هذا المستودع على كود النظام الخلفي (Backend)، تطبيق الجوال (Mobile App)، نماذج الذكاء الاصطناعي (AI Models)، وملفات التوثيق (Documentation).

---

## 📂 هيكلية المشروع (Project Structure)

تم تقسيم المشروع لضمان عدم تداخل العمل، يرجى الالتزام بالمجلد الخاص بمهمتك:

*   **`backend/`**: خاص بـ (Django API & Database). **[المسؤول: عضو 2]**
*   **`mobile_app/`**: خاص كود تطبيق (Flutter). **[المسؤول: عضو 3]**
*   **`ai_engine/`**: خاص بتدريب النماذج وسكريبتات المعالجة (AI/ResNet). **[المسؤول: عضو 4]**
*   **`docs/`**: يحتوي على مستندات التحليل (الفصل الثالث) والتصميم (الفصل الرابع). **[المسؤول: عضو 1 & 5]**

---

## 🚀 البداية (Getting Started)

### 1. تحميل المشروع لأول مرة
افتح التيرمينال (Terminal) في المجلد الذي تريد حفظ المشروع فيه ونفذ:

```bash
git clone <ضع_رابط_المستودع_هنا>
cd MissingPersonsProject
2. إعداد البيئة (حسب تخصصك)
🗄️ متطلبات قاعدة البيانات (للجميع)
نحن نستخدم PostgreSQL. قبل تشغيل الباكند، تأكد من:

تثبيت PostgreSQL على جهازك.

إنشاء قاعدة بيانات محلية فارغة باسم: missing_persons_db.

تأكد من أن لديك صلاحيات الدخول (يفضل استخدام الإعدادات الافتراضية محلياً أو إنشاء ملف .env).

🐍 للمطورين (Backend & AI) - Python
bash
# تفعيل البيئة الافتراضية (Windows)
python -m venv venv
.\venv\Scripts\activate

# تثبيت المكتبات (بما في ذلك psycopg2 للربط مع البوستجرس)
pip install -r backend/requirements.txt
📱 لمطوري الجوال (Flutter)
bash
cd mobile_app
flutter pub get
flutter run
⚠️ قواعد العمل (Git Workflow Rules) - هام جداً!
للحفاظ على الكود نظيفاً وتجنب المشاكل، يمنع الرفع المباشر على فرع main. اتبع الخطوات التالية في كل مرة تبدأ فيها العمل:

الخطوة 1: تحديث نسختك (قبل أن تبدأ أي شيء)
دائماً ابدأ يومك بسحب آخر التعديلات من زملائك:

bash
git checkout main
git pull origin main
الخطوة 2: إنشاء فرع جديد لمهمتك
لا تعمل على الـ main مباشرة. أنشئ فرعاً باسم المهمة التي تعمل عليها:

bash
# مثال: git checkout -b feature/login-screen
git checkout -b <نوع_التعديل>/<اسم_المهمة>
الخطوة 3: العمل والحفظ
بعد الانتهاء من العمل (أو جزء منه):

bash
git add .
git commit -m "وصف مختصر وواضح لما تم إنجازه"
الخطوة 4: رفع التعديلات
bash
git push origin <اسم_الفرع_الذي_أنشأته>
الخطوة 5: طلب الدمج (Pull Request)
اذهب إلى صفحة المشروع على GitHub.

ستجد زر "Compare & pull request".

اضغط عليه واكتب شرحاً بسيطاً، ثم اضغط Create Pull Request.

سيقوم "مدير المشروع" بمراجعة الكود ودمجه مع النسخة الرئيسية.

📝 ملاحظات إضافية
معلومات الاتصال بالقاعدة: لا تقم برفع كلمات مرور قاعدة البيانات (db password) إلى GitHub أبداً. استخدم متغيرات البيئة (Environment Variables) أو ملف .env وقم بإضافته لملف التجاهل.

Migration: عند سحب تحديثات جديدة في الـ Backend، لا تنس تنفيذ python manage.py migrate لتحديث هيكلية قاعدة البيانات المحلية لديك لتطابق PostgreSQL.

التوثيق: أي تعديل في الكود (مثل تغيير اسم حقل في قاعدة البيانات) يجب أن يقابله تعديل فوري في ملف docs/analysis_ch3.

بالتوفيق يا شباب! 💪


### 💡 تلميح سريع لـ (عضو 2 - Backend):
بما أنكم انتقلتم لـ PostgreSQL، ذكّر زميلك (عضو 2) بأنه يجب عليه إضافة مكتبة `psycopg2-binary` إلى ملف `requirements.txt` وتعديل إعدادات `DATABASES` في ملف `settings.py` الخاص بـ Django ليدعم محرك `django.db.backends.postgresql`.
